{
  "name": "gateway-api-mcp",
  "version": "1.0.0",
  "description": "MCP server wrapping the Infragistics AI Agent Gateway — exposes invoke_by_product_type and invoke_master_agent as native MCP tools",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "dev": "ts-node src/index.ts",
    "start": "node dist/index.js"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "~5.4.0",
    "ts-node": "^10.9.0"
  }
}
